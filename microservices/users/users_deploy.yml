apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-microservice-deployment
  labels: 
    app: usersService
spec:
  replicas: 1
  selector:
    matchLabels:
      app: usersService
  template:
    metadata:
      labels:
        app: usersService
    spec:
      containers:
      - name: user-microservice
        image: jomadotio/users_microservice:v1
        ports:
        - containerPort: 3000
        resources:
          requests: 
            memory: "50Mi"
            cpu: "200m"
          limits:
            memory: "200Mi"
            cpu: "1000m"
          

---

apiVersion: v1
kind: Service
metadata:
  name: users-service
spec:
  selector:
    app: usersService
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: LoadBalancer

---

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-autoscaler
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: users-microservice-deployment
  minReplicas: 1
  maxReplicas: 4
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 1
